{
  "name": "atlas-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source runtime for safe, reliable, and verifiable MCP execution.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.5.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
