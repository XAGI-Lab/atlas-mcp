{
  "name": "@atlas-mcp/runtime-core",
  "version": "0.1.0-alpha.0",
  "description": "Durable task lifecycle, budgets, retries, and verification for ATLAS MCP.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@atlas-mcp/policy-core": "workspace:*",
    "@atlas-mcp/protocol": "workspace:*",
    "@atlas-mcp/receipt-schema": "workspace:*",
    "@atlas-mcp/storage-sqlite": "workspace:*",
    "@atlas-mcp/verifier-core": "workspace:*"
  }
}
